
A simple global keyboard auto-press tool built with Python.
Automatically presses a selected key at a fixed interval in any desktop application.

✨ Features
-Global key auto-press
-Popup UI to select key & delay
-F11 → Start / Stop
-Esc → Exit
-Saves last settings
-EXE support


▶️ Run (Python)
```python
pip install keyboard ttkbootstrap
python Auto_clicker.py
```

Run as Administrator

📦 Create EXE
```python
pip install pyinstaller
pyinstaller --onefile --noconsole Auto_clicker.py
```

EXE location:

dist/Auto_clicker.exe

⚠️ Notes

Windows only

Does not work in games with kernel anti-cheat

For automation & learning purposes only
